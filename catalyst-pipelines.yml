version: 1
jobs:
  hello:
    steps:
      - echo "Hello"
  world:
    steps:
      - echo "World"
  hello-world:
    steps:
      - echo "Hello World!"
  git:
    runner: run
    steps:
      - git add .
      - git commit -m "test"
      - git push -u origin main
stages:
  - name: build
    jobs:
      - - hello
        - world
      - hello-world
      - git
runners:
  run:
    config-id: 1